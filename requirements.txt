# QuantumSpectre Elite Trading System - Dependencies

# Core dependencies
python-dotenv>=0.21.0
pyyaml>=6.0
toml>=0.10.2
uvicorn[standard]>=0.20.0 # Use [standard] for websockets, http-tools
# uvloop>=0.17.0; sys_platform != 'win32' # Optional, for performance on Linux/macOS
websockets>=10.4 # For FastAPI WebSocket support if not covered by uvicorn[standard]
aiohttp>=3.8.3 # Used by some components (e.g., ws_client, rest_client)
asyncio>=3.4.3 # Standard library
fastapi>=0.89.1
pydantic>=1.10.4
numpy>=1.24.1
pandas>=1.5.3
scipy>=1.10.0 # For statistical calculations
psycopg2-binary>=2.9.5 # For PostgreSQL
redis>=4.4.2 # For async Redis, ensure it's redis.asyncio or compatible
httpx>=0.23.3 # Modern HTTP client
python-multipart>=0.0.5 # For FastAPI file uploads
requests>=2.28.2 # Standard HTTP client, though httpx is preferred for async

# Data processing and visualization
matplotlib>=3.6.3
seaborn>=0.12.2
plotly>=5.13.0
# dash>=2.8.1 # If Dash UI is used
# streamlit>=1.17.0 # If Streamlit UI is used

# Machine learning
scikit-learn>=1.2.1
xgboost>=1.7.3
lightgbm>=3.3.5
catboost>=1.1.1
tensorflow>=2.11.0 # Consider tensorflow-cpu if GPU is not intended
# tensorflow-cpu>=2.11.0; sys_platform == 'win32' # Alternative for Windows
torch>=1.13.1 # Consider torchvision, torchaudio if needed
transformers>=4.26.0 # For NLP models
joblib>=1.2.0 # For saving/loading sklearn models
shap>=0.41.0 # For model explainability
# optuna>=3.1.0 # If used for hyperparameter optimization
hyperopt>=0.2.7 # Used in ml_models/training.py
statsmodels>=0.13.0 # For statistical models, used in regime_feed
hmmlearn>=0.2.7 # For Hidden Markov Models, used in regime_feed
dask[complete]>=2022.1.0 # For parallel computing, used in feature_service
# ray[tune]>=2.2.0 # If Ray Tune is used

# Natural language processing
nltk>=3.8.1
# textblob>=0.17.1 # If used
spacy>=3.5.0
# gensim>=4.3.0 # If used

# Crypto and finance specific
# ccxt>=2.6.26 # Comprehensive crypto exchange library, if directly used
# python-binance>=1.0.16 # Specific Binance client, if used directly
# ta>=0.10.2 # Technical Analysis library
# TA-Lib>=0.4.24 # Technical Analysis library (Python wrapper) - Temporarily commented out
# ta-lib>=0.4.25 # TA-Lib, requires C library. Check if used directly or via `ta`
# pyfolio>=0.9.2 # Portfolio analytics, if used
empyrical>=0.5.5 # Financial metrics, if used
# backtrader>=1.9.76.123 # Backtesting framework, if used directly
web3>=6.0.0 # For Ethereum/BSC on-chain data, requires eth-utils>=2.0.0
python-bitcoinrpc>=1.0 # For Bitcoin RPC communication
etherscan-python>=0.2.0 # For Etherscan API interaction
# pycoingecko>=3.1.0 # CoinGecko API client, if used

# Web scraping and data collection
beautifulsoup4>=4.11.2
selenium>=4.8.0 # If used for dynamic scraping
webdriver-manager>=3.8.5 # Helper for Selenium, used in social_feed
trafilatura>=1.0.0 # For web scraping, used in news_feed
newspaper3k>=0.2.8 # For news scraping, used in news_feed
opencv-python>=4.7.0 # For computer vision (cv2), used in pattern recognition
# scrapy>=2.7.1 # If Scrapy framework is used
feedparser>=6.0.10 # For RSS feeds
# tweepy>=4.12.1 # Twitter API v1, consider v2 alternatives
# praw>=7.7.0 # Reddit API
# telethon>=1.26.1 # Telegram API

# Dark Web / TOR
# PySocks needed for aiohttp proxy
PySocks>=1.7.1 # For TOR proxy with aiohttp
stem>=1.8.0 # For TOR control port interaction

# Testing and monitoring
# pytest>=7.2.1
# pytest-asyncio>=0.20.3
# pytest-cov>=4.0.0
# faker>=17.0.0
psutil>=5.9.0 # For system and process utilities (used in common/metrics.py)
prometheus-client>=0.16.0 # If Prometheus monitoring is active
# sentry-sdk>=1.15.0 # If Sentry is used for error tracking
# opentelemetry-api>=1.15.0 # If OpenTelemetry is used
# opentelemetry-sdk>=1.15.0

# Security
cryptography>=39.0.0 # For Fernet encryption
# passlib[bcrypt,argon2]>=1.7.4 # For password hashing, ensure extras are installed if needed
python-jose[cryptography]>=3.3.0 # For JWT
# bcrypt>=4.0.1 # If passlib doesn't cover it or used directly
# argon2-cffi>=21.3.0 # If passlib doesn't cover it or used directly

# Documentation
# sphinx>=6.1.3
# mkdocs>=1.4.2
# jupyter>=1.0.0

# Development tools
# black>=23.1.0
# isort>=5.12.0
# flake8>=6.0.0
# mypy>=1.0.0
# pre-commit>=3.0.1

# Deployment
# gunicorn>=20.1.0 # If used for serving FastAPI/Flask apps
# docker>=6.0.1 # If Docker is used
# kubernetes>=26.1.0 # If Kubernetes is used

# Other specific imports found
PyJWT>=2.6.0 # Used in api_gateway/app.py for JWT
SQLAlchemy>=1.4.0 # Used in common/database.py and common/models.py
# alembic>=1.9.0 # For database migrations, if used with SQLAlchemy
# psycopg2-binary>=2.9.5 # Already listed, for PostgreSQL with SQLAlchemy
asyncpg>=0.27.0 # Used by common/db_client.py for async PostgreSQL
# pdfkit # For PDF report generation in backtester/report.py
# jinja2 # For HTML templating in backtester/report.py
# talib-binary # Alternative for TA-Lib if precompiled binaries are preferred
# hmmlearn # For HMM in regime_feed.py

# UI related (if backend needs to serve/build UI, otherwise these are frontend dev dependencies)
# html-webpack-plugin
# mini-css-extract-plugin
# copy-webpack-plugin
# terser-webpack-plugin
# dotenv-webpack
# webpack-bundle-analyzer
# @pmmmwh/react-refresh-webpack-plugin
# babel-loader
# @babel/preset-env
# @babel/preset-react
# @babel/preset-typescript
# react-refresh
# @babel/plugin-transform-runtime
# @babel/plugin-proposal-class-properties
# worker-loader
# style-loader
# css-loader
# postcss-loader
# postcss-preset-env
# tailwindcss
# autoprefixer
# sass-loader
# csv-loader
# xml-loader
# file-loader
# raw-loader
# crypto-browserify # Webpack fallbacks
# stream-browserify
# buffer
# path-browserify
# os-browserify
